mutation CreateUser($userInput: UserInput!) {
  createUser(userInput: $userInput) {
    id
    displayName
    walletAddress
    birthday
    bio
    avatarUrl
    genderType
    genderValue
    interests
    createdAt
    updatedAt
  }
}
