mutation CreateUser($input: CreateUserInput!) {
  createUser(userInput: $input) {
    id
    displayName
    walletAddress
    birthday
    bio
    avatarUrl
    genderType
    genderValue
    interests
    createdAt
    updatedAt
  }
}
